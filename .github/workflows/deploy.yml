name: Deploy para GitHub Pages

on:
  push:
    # A branch de revisão publica uma prévia no Pages antes do merge.
    # Remover agent/revisao-ux-features desta lista antes de concluir o PR.
    branches: ["main", "agent/revisao-ux-features"]

concurrency:
  group: "pages"
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      # URL pública não é segredo e deve ficar explícita no build publicado.
      VITE_PUBLIC_APP_URL: https://bonifaciogui.github.io/controle-de-treino/
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install and Build
        run: |
          npm install
          npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy
        uses: actions/deploy-pages@v4
